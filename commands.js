/* global Office */

// Initialize Office
Office.onReady(() => {
  console.log("Commands.js initialized");
});

// This file can contain additional command handlers if needed
// Currently, most event handling is done in launchevent.js